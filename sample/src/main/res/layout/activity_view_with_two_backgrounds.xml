<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <es.voghdev.progressbuttonview.ProgressButtonView
        android:id="@+id/progressButtonView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        app:hideButtonWhileLoading="false"
        app:text="@string/visible_promise"
        app:backgroundColorResource="@color/colorPrimary"
        />
    <!-- The android:background property is a wrong attribute.
    We are testing that ProgressButtonView does not crash when this attribute is set.
     It just ignores the attribute. -->
</LinearLayout>
